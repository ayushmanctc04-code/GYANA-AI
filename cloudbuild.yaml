steps:
  - name: gcr.io/cloud-builders/docker
    args:
      - build
      - -t
      - ${_IMAGE}
      - .

images:
  - ${_IMAGE}

substitutions:
  _IMAGE: us-central1-docker.pkg.dev/$PROJECT_ID/gyana-ai/backend:latest
